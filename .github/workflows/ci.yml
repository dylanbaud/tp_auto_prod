name: CI

on: 
  push:
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - uses: php-actions/composer@v6 # ou alternative pour la gestion des dépendances
    - uses: php-actions/phpunit@v3
      with:
        args: --coverage-text --configuration phpunit.xml
